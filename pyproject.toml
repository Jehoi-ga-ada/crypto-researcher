[project]
name = "crypto-researcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.2.12",
    "langgraph>=1.1.1",
    "langsmith>=0.7.16",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.2",
]
